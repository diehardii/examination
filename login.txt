请用springboot+MVC+Mybatis+vue前后端分离的机构实现一个登陆注册功能的模块，请给出完整的代码，用到的数据库表结构如下：
create table examinai.users
(
    id       bigint auto_increment
        primary key,
    password varchar(255) not null,
    phone    varchar(255) not null,
    username varchar(255) not null,
    role_id  int          not null,
    constraint UKdu5v5sr43g5bfnji4vb8hg5s3
        unique (phone),
    constraint UKr43af9ap4edm43mmtq01oddj6
        unique (username),
    constraint FKp56c1712k691lhsyewcssf40f
        foreign key (role_id) references examinai.roles (role_id)
);

create table examinai.roles
(
    role_id   int auto_increment
        primary key,
    role_name varchar(255) not null,
    constraint UK716hgxp60ym1lifrdgp67xt5k
        unique (role_name)
);
版本为Spring Boot 3.x / Spring Security 6.x
请用最最简单的安全认证方式，要求安全级别最低
数据库映射请用mapper 不要用repository