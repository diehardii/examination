左侧菜单栏
<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">

</head>
<body>
<div th:fragment="menu" class="sidebar">
    <h2>智能考试管理系统</h2>
    <ul class="menu" th:if="${user.roleId == 1}">
        <!-- 管理员菜单 -->
        <li><a th:href="@{/exam-paper/paper-gen}">📄 试卷生成</a></li>
        <li><a th:href="@{/exam-paper/paper-man}">📚 试卷管理</a></li>
        <li><a th:href="@{/exam-paper/paper-test}">📝 试卷考试</a></li>
        <li><a th:href="@{/exam-paper/paper-ana}">📊 试卷分析</a></li>
        <li><a th:href="@{/exam-paper/question-ana}">💡 试题分析</a></li>
        <li><a th:href="@{/exam-paper/subjects}">🧰 科目管理</a></li>
        <li><a th:href="@{/auth/role-man}">⚙️ 系统管理</a></li>
        <li>

            <form class="logout-form" th:action="@{/logout}" method="post">
                <input type="hidden" th:name="${_csrf.parameterName}" th:value="${_csrf.token}"/>
                <button type="submit" class="logout-btn">🚪 退出系统</button>
            </form>

        </li>
    </ul>
    <ul class="menu" th:unless="${user.roleId == 1}">
        <!-- 普通用户菜单 -->
        <li><a th:href="@{/exam-paper/paper-man}">📚 试卷管理</a></li>
        <li><a th:href="@{/exam-paper/paper-test}">📝 试卷考试</a></li>
        <li><a th:href="@{/exam-paper/paper-ana}">📊 试卷分析</a></li>
        <li><a th:href="@{/exam-paper/question-ana}">🔍 试题分析</a></li>
        <li>

            <form class="logout-form" th:action="@{/logout}" method="post">
                <input type="hidden" th:name="${_csrf.parameterName}" th:value="${_csrf.token}"/>
                <button type="submit" class="logout-btn">🚪 退出系统</button>
            </form>

        </li>
    </ul>
</div>
</body>
</html>

主页面
<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
  <meta charset="UTF-8">
  <title>智能考试管理系统 - 主页</title>
  <link rel="stylesheet" th:href="@{/css/menuStyle.css}">
  <style>
    .container {
      display: flex;
      min-height: 100vh;

    }

    .main-content {
      flex-grow: 1;
      padding: 20px;
      display: flex;
      align-items: center; /* 垂直居中 */
      justify-content: center; /* 水平居中 */
      height: calc(100vh - 40px); /* 减去padding */
    }

    .main-content img {

      width: 800px !important;  /* 固定宽度 */
      height: 600px !important; /* 固定高度 */
      object-fit: contain; /* 保持比例完整显示 */
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
    }
  </style>
</head>
<body>
<div th:replace="~{fragments/menu :: menu}"></div>

<div class="content">
  <div class="main-content">
    <img th:src="@{/images/menu.jpg}" alt="系统功能示意图">
  </div>
</div>
</body>
</html>